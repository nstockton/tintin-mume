#python

import secrets

#gts

#alias {ddo} {
	#python secrets.setRoomName("${roomName}");
	#python secrets.actionAll("open")
}
#alias {dadd} {
	#python secrets.setRoomName("${roomName}");
	#python secrets.add("%1", "%2")
}
#alias {ddel} {
	#python secrets.setRoomName("${roomName}");
	#python secrets.delete("%1", "%2")
}
#alias {dinfo} {
	#python secrets.setRoomName("${roomName}");
	#python secrets.info("%0")
}

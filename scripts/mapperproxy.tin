#run {mapper} {python3 -B}

from mapperproxy.mapper.main import main

#action {^MPICOMMAND:%1:MPICOMMAND$} {#mume {#system %1;#mapper continue}}

#gts

#mapper main(outputFormat="tintin", interface="text", isEmulatingOffline=False, promptTerminator=None, gagPrompts=False, findFormat="{vnum}, {name}, {attribute}", localHost="127.0.0.1", localPort=4000, remoteHost="mume.org", remotePort=4242, noSsl=False)
